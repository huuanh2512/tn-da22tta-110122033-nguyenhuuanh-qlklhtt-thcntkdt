name: review_module
description: "A new Flutter package project."
version: 0.0.1
publish_to: "none"

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  get_it: ^8.0.2
  go_router: ^14.2.0
  equatable: ^2.0.5
  dartz: ^0.10.1
  server_module:
    path: ../server_module
  notification_module:
    path: ../notification_module

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
